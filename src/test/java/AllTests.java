import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({ ClienteTest.class, EmpresaTest.class})
public class AllTests {
    //Los test están mal hechos, pero por falta de tiempo y por habernos organizado mal, no podemos cambiarlos.
}
